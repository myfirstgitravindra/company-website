// This file lives in your GitHub repo
pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                checkout scm  // Automatically checks out the repo
            }
        }
        // ... rest of pipeline
    }
}